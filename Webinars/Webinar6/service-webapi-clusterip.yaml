apiVersion: v1
kind: Service
metadata:
  name: myapp-webapi-service
  namespace: webinar6
  labels:
    app: myapp-webapi
spec:
  type: ClusterIP
  selector:
    app: myapp-webapi
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
