apiVersion: v1
kind: Service
metadata:
  name: myapp-webapp-service
  namespace: webinar6
  labels:
    app: myapp-webapp
spec:
  type: ClusterIP
  selector:
    app: myapp-webapp
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
